# 跨平台换行与二进制标记
#
# 为什么需要它：本项目要在 Windows（作者本机）与 Linux/macOS（评委、Docker、
# Render/Fly）上都能跑。Git 的 `core.autocrlf` 在 Windows 上默认把工作区文件
# 转成 CRLF —— 一个带着 CRLF 的 `start_demo.sh` 在 Linux 上会直接报
# `bad interpreter: /usr/bin/env bash^M`，而且**在 Windows 上完全看不出来**。
#
# 规则：仓库里一律存 LF；只有 Windows 的 .cmd/.bat 用 CRLF。

* text=auto eol=lf

# Windows 批处理必须是 CRLF（cmd.exe 对 LF 的容忍度不可靠）
*.cmd text eol=crlf
*.bat text eol=crlf

# 二进制：不要做任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.exe binary
*.zip binary
*.gz binary
*.woff binary
*.woff2 binary

# 数据快照：CSV/JSON 按文本走 LF（一般文件用）
*.csv text eol=lf
*.json text eol=lf

# ══════════════════════════════════════════════════════════════════════════
# 🔴 但 data/ 下的数据**逐字节就是契约**，绝不能让 git 做任何换行转换
# ══════════════════════════════════════════════════════════════════════════
# 为什么（这是一个真实踩到的坑，而且**本机自检全绿**、换台机器就废）：
#
#   `data/SNAPSHOT.md` 里的 SHA256 是按**字节**算的。而本仓库里有一批数据文件
#   上游就是 CRLF（例如 data/derived/*.csv）。如果按上面的 `eol=lf` 规则走：
#     · 仓库里存 LF、检出也是 LF；
#     · 但本机工作副本是 CRLF -> 清单里的哈希是按 CRLF 算的；
#     · 评委 `git clone` 拿到 LF 字节 -> `snapshot_manifest.py --verify`
#       直接报 15 项"冻结项哈希不符"。
#   结果是：**我们用来证明"数据没被篡改"的工具，反而在评委机器上喊"数据被篡改"**。
#   （实测：26 项里有 15 项中招。）
#
# 所以 data/ 一律标成 `-text`（二进制、不做任何转换）：clone 出来的字节
# 与本机**逐字节一致**，SHA256 在任何平台都成立。
# 代价是 `git diff` 对 data/ 下的文件显示"二进制不同"—— 但这正是我们想要的：
# 数据不看行级 diff，看哈希。
data/** -text
