# 密钥：绝不入库
.env
*.env
!.env.example

# 运行时产物
__pycache__/
*.pyc
data/positions/open.json
data/positions/alerts.json
data/reports/_*.txt
# 保活守护的运行期产物（PID / 状态 / 公网地址 / 日志）—— 不进库
data/run/
# 事件驱动降本的状态（T4-B）：运行时产物，不进库（news_state.json 仍由 news_sources.py 管理）
data/derived/event_driven_state.json
_tmp_*

# 外部协作工具的本地状态（不是本项目内容，只存在于本机）
# 说明：这类目录「存在但不该进库」——直接删可能弄坏在用的工具，所以选择**忽略**而不是删。
.workbuddy/
.claude/
.cursor/
.idea/
.vscode/

# cloudflared 可执行文件（52 MB，平台相关，不进库）
# 为什么放 tools/bin：`run_p2.find_cloudflared()` 把它列为**第一优先**路径。
# ⚠️ 实测踩到：以前只放在 %TEMP%，而临时目录**换一个会话就没了** ——
#    于是"上一轮还能开公网隧道，这一轮静默跳过"，日志里只有一行
#    `没找到 cloudflared，跳过隧道`。稳定路径才是对的。
tools/bin/
