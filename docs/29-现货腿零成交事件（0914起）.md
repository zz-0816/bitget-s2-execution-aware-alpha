# rToken 现货腿**自 09-14 起零成交**（实测记录）

> 发现时间 **2026-09-18** ｜ 发现者 **A** ｜ 状态：**已核实，未结论影响**
> 触发原因：做双腿联合分布时发现"现货成交带只到 09-14"，顺着查下去确认了根因
>
> ## 🟢 更新（2026-09-19 06:47 UTC）：**现货腿恢复成交了**
>
> 见下方 **§5**。零成交持续 **5 天**（09-14 00:00 → 09-19 06:46 UTC）后恢复，
> 恢复时刻**落在 `in_house` 窗口内**（窗口 09-19 00:00 UTC 开启）。
> 本文档前面的记录**保留原样**（它是当时的事实），§5 记恢复。

---

## 0. 一句话

**不是采样器漏采，也不是我们的代码问题** —— 上游 `R*USDT`（rToken 现货）
**自 2026-09-14 00:00 UTC 起，8 个标的全部没有任何成交**；
但**报价仍在正常刷新**（09-17 现货点差中位 1.86 bp）。
→ 这是一个**只有报价、没有成交**的市场状态，已持续 **4 天**。

---

## 1. 证据链（三条独立证据，互相印证）

### ① 上游接口直查：最新成交就停在 09-14

```
GET https://api.bitget.com/api/v2/spot/market/fills-history?symbol=RNVDAUSDT&limit=3
```

| symbol | 最新成交时间（UTC） |
|---|---|
| `RNVDAUSDT` | 2026-09-14 00:00:30 |
| `RTSLAUSDT` | 2026-09-14 00:00:41 |
| `RGOOGLUSDT` | 2026-09-13 23:30:04 |
| `RSPYUSDT` | 2026-09-13 21:33:44 |

**同一条接口现在返回的第一行就是 09-14 的那一笔** —— 意味着这 4 天没有新成交。
（顺带确认一个已知坑：`fills-history` 不认 `endTime` 翻页，见 `trades_sampler.py` 头部注释。）

### ② 采样器状态文件：现货时间戳停在 09-13/09-14，永续是新鲜的

`data/spread/.trades_state.json` 里的 `last_ts`：

> ⚠️ 这份**状态文件**（采样器的内部游标）与下面是**当时的读数记录**，
> 两者都**不在本仓库**：状态文件在项目一仓库，`2026-09-17` 那一天的盘口
> 在快照按"最后 400 轮"截断后已不在仓库里（见 `README.md` §3「跨仓库引用约定」）。
> 本节的价值是**事件当时的证据链**，不是"现在还能就地复算"。

| 类别 | 最新 `last_ts` |
|---|---|
| 现货（`R*USDT`，10 个） | `RAAPLUSDT` / `RGOOGLUSDT` = **09-13 23:30**；`RHOODUSDT` / `RMETAUSDT` = **09-14 00:00** |
| 永续（`*USDT`，10 个） | `AAPLUSDT` / `GOOGLUSDT` / `METAUSDT` = **09-18 05:16~05:20** |

→ 采样器**在跑**（永续更新时间就是当前），现货侧是"上游没有新数据"。

### ③ 落盘文件：现货成交笔数逐日归零

| 日期 | 现货笔数 | 永续笔数 |
|---|---|---|
| 2026-09-12 | 572 | 18,724 |
| 2026-09-13 | 5,776 | 61,739 |
| 2026-09-14 | 3,445 | 514,260 |
| 2026-09-15 | **0** | 342,523 |
| 2026-09-16 | **0** | 159,906 |
| 2026-09-17 | **0** | 381,514 |
| 2026-09-18（部分） | **0** | 98,978 |

### ④ 报价仍在（所以不是"市场关了"）

`data/spread/2026-09-17.csv` 里 `NVDA` 现货行 **1,081 行**，点差中位 **1.86 bp**
（09-13 为 3.19 bp）—— **报价是活的，成交是死的**。

> ⚠️ `2026-09-17.csv` **不在本仓库**（快照只保留最后 400 轮；该文件在项目一仓库）。
> 上面这行是**当时**的读数记录。

---

## 2. 这对已有结论意味着什么

| 受影响的结论 | 影响 |
|---|---|
| **双腿联合成交分布**（`docs/14` §9） | 只能测 **09-12~09-14**（现货成交带的最后三天）。之后的日子"两腿都成交"恒为 0 —— 那不是"相关性变化"，是**测不到** |
| **"只成交一腿" 24.9%** | 这是**三天样本**的数字。若当前状态持续，现货腿的实际成交率是 **0**，`P(只成交一腿)` 会**更高**（= 永续腿单边成交），腿风险比报告里写的更差 |
| **可捕获名义额 $1.4k–$125k**（`docs/13`/`docs/14`） | 来自**盘口深度**，不是成交额。报价还在 → 这个数仍然"算得出来"，但**没有成交量支撑**，实际可捕获性存疑 |
| **"现货腿是容量瓶颈"** | 结论**更强**了：不是"成交额小"，而是**根本没有成交** |
| 项目二的执行建议 | 现货腿"挂单成交"这条路径在 09-15 之后**无法成交**；模型因此在 `stockroute` 分层取到的 `P(现货)≈0.03%` 其实**低估了严重性**（真实值是 0） |

⚠️ **不要**把这条读成"策略更好了"。零成交 = 挂单永远不成交 = **赚不到点差**，
而不是"没有逆向选择风险"。挂单策略的收益来源就是成交。

---

## 3. 尚未结论的部分（诚实标注）

1. **原因未知**：是 rToken 做市商撤了、发行方暂停、还是要下架？
   我们只查了成交接口与状态文件，**没有**去查公告（`bitget-signal` 的
   `news-briefing` 可以查，但需要人工读公告，属于下一步）。
2. **是否可恢复未知**：4 天零成交既可能是临时，也可能是终局。
   08-22 起的历史里有连续多日成交（`trades-2026-08-22`~`09-14`），
   所以至少说明**这个市场曾经是活的**。
3. **永续侧不受影响**：永续成交一直正常（09-17 有 38 万笔），
   基差、资金费、执行成本这些**永续侧**结论不受本条影响。
4. **对"窗口"口径的影响**：`in_house` 是周末窗口，09-19（周六）会再开一次；
   到时可以复检现货是否恢复成交 —— **这是最近的一次观察点**。

---

## 4. 复跑（任何人可自查）

```powershell
# ① 上游最新成交（直接看第一行的时间）
python -c "import json,urllib.request;d=json.load(urllib.request.urlopen('https://api.bitget.com/api/v2/spot/market/fills-history?symbol=RNVDAUSDT&limit=3'));print(d['data'][0]['ts'], d['data'][0]['price'])"

# ② 采样器状态（现货 vs 永续的 last_ts）
python -c "import io,json,datetime as dt;st=json.load(io.open('data/spread/.trades_state.json',encoding='utf-8'));f=lambda m:dt.datetime.fromtimestamp(m/1000,dt.UTC).strftime('%m-%d %H:%M');[print('%-14s %s'%(k,f(v))) for k,v in sorted(st['last_ts'].items())]"

# ③ 逐日成交笔数（现货/永续对照）
python tools\joint_fill_analysis.py --date-from 2026-09-12
```

**观测建议**：`python tools\joint_fill_analysis.py --date-from 2026-09-14 --date-to <今天>`
的「覆盖审计」表会把现货为 0 的日子直接标出来 —— 恢复成交的第一天会立刻可见。

---

## 5. 🟢 更新：**09-19 06:46 UTC 现货腿恢复成交**

### 5.1 事实

| 项 | 值 |
|---|---|
| 恢复时刻 | **2026-09-19 06:46:13 UTC**（北京 14:46） |
| 当时 route / session | **`in_house` / `closed`** —— 即**策略唯一可交易的那个时段** |
| 09-19 当日现货成交 | **560 笔**（截至 06:47 UTC，即窗口开启后约 6.8 小时） |
| 对比：零成交期间 | 09-15 ~ 09-18 每天 **0 笔** |
| 对比：恢复前有成交的日子 | 09-12: 572 ｜ 09-13: 5,776 ｜ 09-14: 3,445 |
| 永续侧 | 一直正常（09-19 已 105,343 笔） |

**发现方式**：项目二的自检突然报「现货腿最后成交距今 0.9 分钟」，
与 `docs/29` 记录的"零成交"矛盾 —— 顺着查下去确认是**真的恢复了**。
（这也说明"把当时的数据写死进自检"是错的：现货一恢复，自检就误报失败。
已改成按真实数据分两种情形检查。）

### 5.2 这意味着什么（三层，**不夸大**）

| 层 | 影响 |
|---|---|
| ⭐ **策略可执行性** | 恢复时刻**恰在 `in_house` 窗口内** —— "现货挂单赚点差"这条路径**重新成立**。这是本项目最关键的运行时前提 |
| ⚠️ **"观测"与"结论"要分开** | 09-19 只覆盖**窗口的前 6.8 小时**，不能据此说"已完全恢复"。要看**整个窗口**（到 09-21 00:00 UTC）的持续性 |
| 🔴 **不能用它改结论** | `docs/14` §9 的联合分布仍是 09-12~09-14 那三天的实测值。09-19 是新 regime，**要重测才能进模型**；在重测前不得混用 |

### 5.3 待办（已记入任务清单）

- [ ] 09-19 窗口**结束时**（09-21 00:00 UTC）重测联合分布：
      `python tools\joint_fill_analysis.py --date-from 2026-09-12 --date-to 2026-09-21 --by-route`
      对比"零成交期 vs 恢复期"的 `P(只成交一腿)`
- [ ] 若恢复持续，把 09-19 之后的数据**纳入**联合分布（并重跑
      `tools/joint_fill_check.py` 看最优执行方式是否再变）
- [ ] 报告里如实写：**09-14~09-18 零成交、09-19 恢复** —— 这是一个
      "我们发现异常 → 如实报告 → 持续监测 → 观察到恢复"的完整过程，
      比只报一个静态结论更可信

⚠️ **仍然不知道原因**：§3 的三种假设（场所改造准备 / 流动性事件 / 豁免落地前奏）
一个都没被证实。恢复**削弱了**"终局"的可能，但**不构成**对任一假设的证实。
